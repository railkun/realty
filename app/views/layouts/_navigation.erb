<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to  properties_path , class: "navbar-brand" do %>
        <i class="fa fa-home" aria-hidden="true" style="font-size:130%;"></i>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to new_property_path do %>
            <i class="fa fa-plus-square-o" aria-hidden="true" style="font-size:150%;"></i>
          <% end %>
          <span class="sr-only">(current)</span>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
            <%= form_tag properties_path, :method => "get", id: "search-form"  do %>
              <% @price_types = Property.price_types.map{|prt| [ t("price_type.#{prt[0]}"), prt[1]] } %>
              <%= select_tag :price_type, options_for_select(@price_types)%>
              <% @property_types = Property.property_types.map{|pt| [ t("property_type.#{pt[0]}"), pt[1]] } %>
              <%= select_tag :property_type, options_for_select(@property_types)%>
              <%= text_field_tag :price_from, params[:price_from], class:"form-control", placeholder:"Ціна від..."%>
              <%= text_field_tag :price_to, params[:price_to], class:"form-control", placeholder:"Ціна до..."%>

              <%= submit_tag "Знайти", type: "submit" , class: "btn btn-default" %>
            <% end %>
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to  edit_user_registration_path  do %>
            <i class="fa fa-user" aria-hidden="true" style="font-size:150%;"></i>
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, :method => :delete do %>
            <i class="fa fa-sign-out" aria-hidden="true" style="font-size:120%;">Вихід</i>
          <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
